// import {fetchApi} from "../helpers/api";
//
// export const asyncValidate = (values) => {
//   let response = fetchApi('get', `validate_email`, {email: values.email}).then((response) => {
//     if (response && !response.valid) {
//       throw { email: "Email already exists" };
//     }
//   })
//
//   return response
// };
